#!/bin/bash

# Initialize a new Git repository
git init

# Set Git configuration
git config user.name "Programmer"
git config user.email "Programmer@example.com"
git config --add user.bio "I am a Git learner"
git config --add user.company "Teknoturf"
git config --add user.location "Chennai"
